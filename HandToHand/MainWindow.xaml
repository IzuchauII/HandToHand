<Window x:Class="HandToHand.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HandToHand"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="350" Background="AliceBlue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Visible" Margin="10">
            <TextBlock Name="ChatLog" 
                   Text="Привет! Я твой локальный ассистент HandToHand. Напиши что-нибудь снизу..." 
                   TextWrapping="Wrap" 
                   Foreground="Black" 
                   FontSize="14"/>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">

            <TextBox Name="UserInput" 
                 Width="240" 
                 Height="30" 
                 VerticalContentAlignment="Center" 
                 Margin="0,0,10,0"/>

            <Button Name="SendButton" 
                Content="Отправить" 
                Width="80" 
                Height="30"
                Click="SendButton_Click"/>

        </StackPanel>
    </Grid>
</Window>
